<?php
/**
 * Legacy OpenSRS Domain Importer (stub)
 *
 * Replaced by the provider-agnostic importer at
 * `includes/class-domain-manager-domain-importer.php`.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Intentionally inert to avoid duplicate declarations and accidental rendering.
return;
